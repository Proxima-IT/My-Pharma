# My Pharma - Environment Configuration

# APPLICATION
DEBUG=True
SECRET_KEY=django-insecure-test-key-change-in-production
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.1

# DATABASE (MySQL)
DB_ENGINE=django.db.backends.mysql
DB_NAME=my_pharma_db
DB_USER=root
DB_PASSWORD=2516
DB_HOST=localhost
DB_PORT=3306

# REDIS
REDIS_URL=redis://localhost:6379/0

# CELERY
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/1
CELERY_ACCEPT_CONTENT=json
CELERY_TASK_SERIALIZER=json
CELERY_RESULT_SERIALIZER=json
CELERY_TIMEZONE=Asia/Dhaka

# JWT CONFIGURATION
JWT_SECRET_KEY=test-jwt-secret-change-in-production
JWT_ACCESS_TOKEN_LIFETIME_MINUTES=1440
JWT_REFRESH_TOKEN_LIFETIME_DAYS=7
JWT_ROTATE_REFRESH_TOKENS=True
JWT_BLACKLIST_TOKEN_CLEANUP=True

# OTP CONFIGURATION
OTP_LENGTH=6
OTP_EXPIRY_MINUTES=5
OTP_MAX_RESEND_ATTEMPTS=3
OTP_RESEND_COOLDOWN_HOURS=1

# SECURITY
ACCOUNT_LOCKOUT_THRESHOLD=5
ACCOUNT_LOCKOUT_DURATION_MINUTES=30
PASSWORD_MIN_LENGTH=8

# EMAIL CONFIGURATION
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=noreply@mypharma.com

# SMS GATEWAY (OTP)
SMS_GATEWAY_URL=
SMS_GATEWAY_API_KEY=
SMS_SENDER_ID=MYPHARMA

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
CORS_ALLOW_CREDENTIALS=True

# LOGGING
LOG_LEVEL=INFO
