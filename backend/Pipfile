[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
Django = ">=4.2,<5.0"
djangorestframework = ">=3.14.0"
djangorestframework-simplejwt = ">=5.3.0"
mysqlclient = ">=2.2.0"
PyMySQL = ">=1.1.0"
redis = ">=5.0.0"
django-redis = ">=5.4.0"
celery = ">=5.3.0"
PyJWT = ">=2.8.0"
cryptography = ">=41.0.0"
python-dotenv = ">=1.0.0"
pydantic = ">=2.5.0"
channels = ">=4.0.0"
daphne = ">=4.0.0"
drf-yasg = ">=1.21.7"
django-ratelimit = ">=4.1.0"
django-auditlog = ">=2.3.0"
django-filter = ">=23.5.0"
django-cors-headers = ">=4.3.0"

[dev-packages]
pytest = ">=7.4.0"
pytest-django = ">=4.7.0"
pytest-cov = ">=4.1.0"
flake8 = ">=6.1.0"
black = ">=23.11.0"

[requires]
python_version = "3.10"

[scripts]
server = "python manage.py runserver 0.0.0.0:8000"
migrate = "python manage.py migrate"
celery = "celery -A my_pharma worker -l info"
shell = "python manage.py shell"
test = "pytest"
